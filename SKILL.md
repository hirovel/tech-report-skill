---
name: tech-report
description: 取证式技术报告写作。起草或润色技术报告正文、写源码取证底稿、下「某个项目有/没有某设计」这类跨项目对比断言、引用闭源产品或二手拆解材料时使用。每条断言绑定一手来源并标注可考证程度，只描述决策不评优劣。
---

# 取证式技术报告写作

目标读者是半年后的自己：每句话都要能看出它查证到什么程度、依赖什么前提、什么时候会失效。

## 一、取证

动材料之前写预期清单：这个模块各家会怎么设计、为什么这么认为，每条注明依据是文档、别家做法还是直觉。进材料后逐条标 证实 / 证伪 / 部分成立 / 没查到。**被证伪的预期是最有价值的产出**，它标出了直觉失灵的位置。清单原文留在底稿里，事后不修饰。

两条硬规矩：

- 不读源码就不写「某家没有」，只写「未在 X 中找到」，并注明 X 是文档还是源码
- 闭源对象一律时间戳化：「据流出源码 v2.1.88 当时……」

底稿逐条带 `路径:行号`，「存疑」一节非空；正文引用源码只给文件路径。材料分级、涉及声誉的断言、版本确认见 [`evidence.md`](evidence.md)。

本地 grep 能验证的自己动手。每轮最多开一路调查，开之前说清楚「缺这条会写不下去哪一句」。

## 二、论证

**三问**替代优劣评价：它防什么具体失败（要能描述成可复现的场景）、代价是什么、成立的前提是什么。前两问答不出来写「未能找到」，第三问必须答。差异归因到前提不同。

不评优劣禁的是没有证据的排名，不禁下结论。证据指向哪就写到哪，一节结尾要有一句自己的结论，说明读完这节读者的判断变了什么。把两边的代价量化摆出来之后，结论跟着证据走，不用停在「见仁见智」。

**因果四格**：每个「因为 / 是为了 / 原因是」都要能填满 actor + action + condition + result，填不满就删掉这个因果。关于材料自身结构的断言同样要给依据（「第二句在给第一句划边界」「这两句说的是一件事」），指不出依据就删。作者没给理由时写「找不到 X 自己的解释」。

**主线是一个论证，不是一份清单。** 动笔前把主线写成一句能被反驳的结论（例：这套机制真正买到的是接入外部系统的能力，不是换掉自己任何部件的自由），每一段推进它一步，段末读者的判断要比段首挪动一点。「有多少」「分几类」「兑现到什么程度」这类清单式主线不合格，它产出的是逐项点名，每段只加事实不推进论点。

允许并鼓励显式的论证转折：「麻烦的是」「另一点是」「反过来看」「综上」。转折词标出的是论点的关节，读者靠它知道自己走到哪了。同一批数字只进入叙述一次，引入之后就地讲完，不要隔几段回头再讲一遍。段落顺序照主线排，不照核对顺序排。

**段与段靠具体物交接。** 下一段第一句抓住上一段留下的一个具体东西（那张表、那 78 行、表里的那个数字）再往前走，不要另起一个抽象命题。承诺句紧贴它承诺的内容：写了「下面逐档清点」，下一段就得开始清点，中间不插定义或岔路。

**拆句验证**，用于「厂商声称 X」这类题目。核对时按这个顺序走：引原文 → 拆成可分别验证的几句 → 抠限定词 → 逐句验证。**落到纸上要倒过来**：第一段就给能验证的事实和数字，宣传语的分寸放在事实之后一两句交代完。核对顺序照搬成叙述顺序，产出的就是一篇文本细读。

## 三、文字

句子层面照 Anthropic 工程博客（anthropic.com/engineering）：开篇第一句就是判断，不铺垫（*Harnesses encode assumptions that go stale as models improve.*，Scaling Managed Agents 首句）；术语第一次出现用括号原地定义，几个术语一口气定义完再往下走；加粗克制。它用问句做小节标题，这一条不学。

篇章层面照本项目 `00-总览.md` 的 0.1：**论点在散文里，证据进条目里。** 一段散文立一个论点，紧跟几条带出处的实例把它顶住（0.1 用 Kimi K3 的说明、gpt-5.3-codex 的指南、72.9 对 73.7 这三条顶住「训练能把 harness 内化」）。论点和证据混在同一句里，段落就会又密又没方向。

正文里的比喻可以用，用来给一个新概念安一个落点（0.1 的缸中之脑、肌肉记忆、马具本义），一节一两个，用完就回到事实。标题里不用比喻。

**删除测试**，逐句问：删掉这句，读者少知道什么？答不上来就删。空转的评价词、过渡句、铺垫、复述、对刚说过的话的换句重说，都在这一关掉下去。定稿前整节再走一遍，砍掉两成字数而事实不少，才算过。

**每段第一句是能被反驳的陈述句。** 反问、设问、「那么问题来了」这类过不了这一关。

还有两类句式写出来就改：靠否定推进（「A 不是 B」「不是 A 而是 B」「真正的 X 是 Y」），改法是直接陈述它是什么；为节奏造的排比，并列实例可以连排。

一节最多一张表，表里只放能横向比较的量。

命名用完整的书面短语，让人不点开就知道内容：「判停」写成「完成的信号」，「模型要调工具」写成「模型请求工具使用」。不用比喻和双关，不用口语疑问式，不省字。

## 四、复核

写完单独走一遍，按顺序，不跳级：

1. **读者** —— 第一个可验证的事实在第一段出现了吗？前置概念给足了吗？术语第一次出现有定义吗？
2. **结构** —— 主线那句话是个能被反驳的结论吗？每段都让读者的判断挪动了吗？段首句抓住了上一段的具体物吗？结尾有自己的结论句吗？
3. **事实** —— 每条断言能回溯到底稿吗？未知的地方标出来了，还是猜了一个？
4. **论证** —— 每个因果和结构断言填得满四格吗？代价有实物或数字顶着吗？
5. **句子** —— 每句都过了删除测试吗？每段第一句是陈述句吗？有否定式定义吗？
6. **术语** —— 同一个东西前后同名吗？

前四级发现的问题必须改。后两级与事实冲突时让步：顺口永远排在事实、限定词和引用绑定之后。已经查证过、写得对的句子不重写。

## 材料仓库

中转材料被新取证取代的整段删掉，留痕写进变更记录。已核实为错的移进负面清单，写清错在哪。正文写完后删掉一次都没被用到的。
