# 取证规矩

[`SKILL.md`](SKILL.md) 第一节的展开：材料分几级、哪类断言要走额外的门。

## 来源层级决定结论强度

动笔前给每个被写的对象定级，写进那一块底稿的开头。

| 可考证程度 | 典型材料 | 允许下的结论 |
|---|---|---|
| 一手源码 | 开源仓库，本地已克隆并锚定 commit | 可断言有无 |
| 泄露的原始源码 | 误带 sourcemap 的发行包，带原始变量名和结构 | 可断言，只对那个快照成立 |
| 反混淆产物 | 解开混淆的发行代码，真实但难读 | 可断言，注明版本号 |
| 官方文档 | 厂商自己写的行为承诺 | 按文档陈述，注明是文档 |
| 第三方复刻 | 别人照行为重新实现一个「类 X」 | 不作证据，那是作者的猜测 |
| 第三方拆解 | 社区逆向文章、行为统计 | 只作线索，指引去哪查 |

混用会让整份报告失去价值——读者以为在看 X 的设计，看到的却是某个网友的设计。作者没读过的材料不进致谢和引用。

闭源对象的时间戳化有一个例外：官方文档明确写出的行为（事件表、判定偏序）可以按文档陈述，那是厂商自己的承诺。

## 可考证程度不对等会偏向读得最细的那一家

一次真实教训。第一版笔记的核心论断是「只有 A 家用某种手法做到了顺序无关」，这条是错的：B 家用另一种归约同样顺序无关，C 家的官方文档明说了并行执行再归并。错因是只逐行读了 A 家、另外几家读的是文档，然后把「我在别家文档里没看到」当成了「别家没有」。

「没有 X」类断言还要写明查找方式——查了哪些位置、用什么方式查的。另一次教训：调研断言某文件没有文档注释，实际只查了文件顶部，注释在函数头上。

## 涉及声誉的断言要走四道门

会让读者觉得某个项目「说一套做一套」的断言——文档与实现不符、宣称未兑现、留了缺口——都是在指控具体的人做了不实陈述，而发布时用的是真实姓名。四条全满足才能进正文：

1. 主线亲手核过，不接受取证 agent 的转述
2. 比对现行文档与现行代码。带日期的历史归档、变更记录、旧版设计稿都不算现行文档
3. 先找善意解释：空实现有没有注释说明理由？未挂载是不是因为它本来就是诊断工具？有没有更晚的笔记记录了调整？
4. 措辞中性。写「这个不变量在发行配置里不挂载，包 README 说明了它是诊断工具」，不写「文档比代码强」

一次真实教训：取证 agent 报了三处「说明比代码强」，主线逐条核对后，两处是拿历史笔记比对当前代码而当前 README 与代码完全一致，第三处未核实，三条全部撤下。

## 版本与重查

只 `fetch` 不动工作区，接着读文件读到的是旧版内容。先确认工作区停在哪个 commit 再读。

结论锚定的 commit 和文档版本记进变更记录，并维护一张重查触发器表：某对象发新版 → 该对象各栏；闭源对象出现新泄露或反混淆分析 → 全部时间戳化断言可升级；官方文档改版 → 所有引文档处；新增一个对比对象 → 每块加一节、每章对照表加一列。

## 标注粒度

来源标注集中到每块开头的一张小表，正文里不逐句加免责声明，只在结论强弱有实质差别的地方就地说明。章末列一节参考资料，只列外部一手材料，每条给标题、链接、日期；滚动更新的活文档单独标出对应版本和取回日期。
