# 取证规矩

[`SKILL.md`](SKILL.md) 第 2 步的展开：材料分几级、每级允许下什么结论、哪几类断言要走额外的门。

## 一、来源层级决定结论强度

同一句话的可信度取决于它站在哪一级材料上。动笔前先给每个被写的对象定级，写进那一块底稿的开头：

| 可考证程度 | 典型材料 | 允许下的结论 |
|---|---|---|
| **一手源码** | 开源仓库，本地已克隆并锚定 commit | 可断言有无 |
| **泄露的原始源码** | 误带 sourcemap 的发行包，带原始变量名和结构 | 可断言，但只对那个快照成立 |
| **反混淆 / 反编译产物** | 解开混淆的发行代码，是真实发行代码只是难读 | 可断言，注明版本号 |
| **官方文档** | 厂商自己写的行为承诺 | 可按文档陈述，注明是文档不是代码 |
| **第三方复刻 / 重写** | 别人照行为重新实现一个「类 X」 | **不能当证据**。那是作者的猜测 |
| **第三方拆解分析** | 社区的逆向文章、行为统计 | 只作线索，指引去哪查，不进正文 |

混用会让整份报告失去价值——读者以为在看 X 的设计，实际在看某个网友的设计。作者没读过的材料不出现在致谢和引用里。

行为观察类的社区分析（流量占比、分级数量这类统计推断）如果确实要引，给出处链接并注明它是推断不是读代码得来的。

### 可考证程度不对等会系统性偏向读得最细的那一家

这是最容易翻车的地方，来自一次真实教训。第一版笔记的核心论断是「只有 A 家用某种手法做到了顺序无关」，这条是错的：B 家用另一种归约同样顺序无关，C 家的官方文档明说了并行执行再归并。

错因不是想吹 A 家，是**只逐行读了 A 家，另外几家读的是文档**，然后把「我在别家文档里没看到」当成了「别家没有」。

> **硬规矩：不读源码，就不写「某家没有」。**
> 只能写「未在 X 中找到」，并注明 X 是文档还是源码。

「没有 X」类断言还必须写明查找方式——查了哪些位置、用什么方式查的。另一次教训：调研断言某文件没有文档注释，实际只查了文件顶部，注释在函数头上。

## 二、闭源对象一律写成时间戳化陈述

闭源产品的当前实现无法核对，泄露源码只证明「某个版本曾经是这样」。

| 不写 | 写成 |
|---|---|
| X 用 5 级压缩整形器 | 据泄露源码分析，**v1.0.x 时**它的压缩分 5 级 |
| X 没有某功能 | **未在泄露源码和官方文档中找到**该功能 |

唯一例外是官方文档明确写出来的行为（事件表、判定偏序这类），可以按文档陈述，因为那是厂商自己的承诺。

这不是保守是准确：闭源产品随时会改，时间戳化的陈述半年后依然成立，无时间戳的会变成谎话。

## 三、涉及项目声誉的断言要走四道门

凡是会让读者觉得某个项目「说一套做一套」的断言——文档与实现不符、宣称未兑现、留了缺口——都是在指控具体的人做了不实陈述。写错就是无故抹黑，而且发布时用的是真实姓名。

这类断言进正文之前必须满足全部四条：

1. **主线亲手核过**，不接受取证 agent 的转述
2. **比对的是现行文档与现行代码**。带日期的历史归档、已实现变更的记录、旧版设计稿都不算现行文档——设计演进不是言行不一
3. **先找善意解释**。空实现有没有注释说明理由？未挂载是不是因为它本来就是诊断或开发期工具？有没有更晚的笔记记录了后来的调整？找不到善意解释再写
4. **措辞中性**。写「这个不变量在发行配置里不挂载，包 README 说明了它是诊断工具」，不写「文档比代码强」

一次真实教训：取证 agent 报了三处「说明比代码强」，主线逐条核对后，两处是它拿历史笔记去比对当前代码，而当前的包 README 与代码完全一致；第三处未核实。三条全部撤下。

## 四、检出版本要先确认再读

核对上游更新时，只 `fetch` 不会动工作区，接着读文件读到的是旧版内容。先确认工作区停在哪个 commit，再读文件。

结论锚定的 commit 和文档版本记进变更记录，并维护一张**重查触发器**表：外界什么事件发生时，哪些结论需要重查。

| 触发事件 | 需要重查 |
|---|---|
| 某对象发新版（现锚定 `<commit>`） | 该对象各栏；重点列出已知的待定项 |
| 闭源对象出现新的泄露或反混淆分析 | 全部「某版本当时」的时间戳化断言可升级 |
| 官方文档改版 | 所有引文档处 |
| 新增一个对比对象 | 每块加一节 → 每章对照表加一列 |

## 五、标注粒度

- **正文引用源码给到文件路径**，不写行号
- **底稿仍要给 `路径:行号`**——那是核对物证，不是读物
- **来源标注集中到每块开头的一张小表**，正文里不逐句加免责声明，只在结论强弱有实质差别的地方就地说明
- 正文每条断言能回溯到底稿的对应条目
